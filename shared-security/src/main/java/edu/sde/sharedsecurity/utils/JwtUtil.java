package edu.sde.sharedsecurity.utils;

// shared-security/src/main/java/com/oauth/pkce/shared/security/util/JwtUtil.java
@Component
public class JwtUtil {
    public String parseSubject(String token) {
        // Common JWT parsing logic
    }
    
    public boolean validateToken(String token, String expectedIssuer) {
        // Common token validation
    }
}